# Kindle 霸屏日程看板 — 需求与技术方案

## 一、项目概述

将闲置 Kindle（已越狱）改造为一块常驻显示的日程看板，家人通过手机微信或网页表单发送自然语言消息即可新增日程，Kindle 端自动解析并显示为 Apple 风格卡片，支持触摸滑动切换日期。页面布局支持远程 OTA 更新，无需重新连线 Kindle。

## 二、需求分析

### 2.1 用户画像

| 角色 | 场景 | 技术能力 |
|------|------|---------|
| 日程使用者（老年人） | 微信发一句话即可新增日程 | 仅会使用微信 |
| 管理者（家属） | 修改页面布局、调整解析规则 | 能改 HTML/CSS、重启服务 |

### 2.2 功能需求

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| F-01 | Kindle 全屏显示日程看板 | 必须 | 霸屏模式，无浏览器地址栏/工具栏/标题栏 |
| F-02 | 防误触锁定 | 必须 | 老年人点击屏幕任意区域不会跳出页面 |
| F-03 | 手机微信发消息新增日程 | 必须 | 微信公众号回调，零学习成本 |
| F-04 | 手机网页表单新增日程 | 必须 | 自然语言输入 + 结构化表单双模式 |
| F-05 | 自然语言解析为结构化字段 | 必须 | 正则匹配日期/时间/标题/备注 |
| F-06 | 日程按日期分组、按时间排序 | 必须 | 卡片形式展示 |
| F-07 | 触摸滑动切换日期 | 必须 | 左滑下一天、右滑上一天 |
| F-08 | 定时轮询获取最新数据 | 必须 | Kindle 端 JS 每 30 秒拉取一次 |
| F-09 | 页面布局远程 OTA | 必须 | 改服务端 HTML/CSS，Kindle 下次轮询自动生效 |
| F-10 | 解析规则远程更新 | 理想 | 改服务端正则代码，重启服务即可 |
| F-11 | 日程标记完成/删除 | 理想 | API 已预留，UI 暂不实现 |
| F-12 | 开机自启 | 理想 | Kindle 断电恢复后自动进入 kiosk |

### 2.3 非功能需求

| 编号 | 需求 | 说明 |
|------|------|------|
| NF-01 | 常亮常电 | 不考虑续航，Kindle 始终插电 |
| NF-02 | e-ink 优化 | 白底黑字最大对比度，减少不必要的全屏刷新 |
| NF-03 | Apple 设计风格 | 圆角卡片、药丸标签、Helvetica 字体、灰底分层 |
| NF-04 | 老年人友好 | 大字号、大按钮、最少操作步骤 |
| NF-05 | 低运维 | 服务端零依赖（Node.js + SQLite），无需数据库运维 |

### 2.4 约束条件

- Kindle 已越狱（或确定会越狱），不考虑不越狱的方案
- 服务器部署方案暂不纳入本文档范围
- 不考虑续航问题，Kindle 始终插电
- 不使用第三方前端框架（Kindle 浏览器引擎老旧，用原生 HTML/CSS/JS）

## 三、系统架构

### 3.1 整体架构图

```
┌─────────────┐         ┌──────────────────────────┐         ┌──────────────┐
│   手机端    │         │        服务端            │         │   Kindle 端  │
│             │         │                          │         │              │
│  微信公众号 │──消息──▶│  微信回调接收             │         │              │
│  (发一句话) │         │         │                │         │              │
│             │         │         ▼                │         │              │
│  网页表单   │──HTTP──▶│  正则解析模块            │         │              │
│  (自然语言  │         │         │                │         │              │
│   或表单)   │         │         ▼                │         │              │
│             │         │  SQLite 存储             │         │              │
│             │         │         │                │         │              │
│             │         │    ┌────┴────┐           │         │              │
│             │         │    ▼         ▼           │         │              │
│             │         │  REST API  HTML 页面     │◀─轮询──│  浏览器 Kiosk │
│             │         │  (JSON)   (dashboard)    │  30秒   │  (全屏无UI)  │
│             │         │                          │         │              │
└─────────────┘         └──────────────────────────┘         └──────────────┘
```

### 3.2 数据流

**新增日程流：**

```
用户操作 → 消息进入 → 正则解析 → 存入 SQLite → Kindle 下次轮询拿到新数据
```

**Kindle 显示流：**

```
Kindle 浏览器 → GET /dashboard 加载 HTML 页面
             → JS 每 30 秒 GET /api/schedules 拉取 JSON 数据
             → 渲染为 Apple 风格卡片
             → 触摸滑动切换日期
```

### 3.3 技术选型

| 层 | 技术 | 选型理由 |
|----|------|---------|
| 后端框架 | Node.js + Express | 轻量、单文件部署、SQLite 原生支持 |
| 数据存储 | SQLite | 零运维、单文件数据库、无需安装数据库服务 |
| 消息解析 | 正则表达式 | 够用、可迭代、不引入 NLP 依赖 |
| 微信对接 | xml2js + 原生 crypto | 解析微信 XML 消息体 + SHA1 签名校验 |
| 前端 | 原生 HTML/CSS/JS | Kindle 浏览器基于旧版 WebKit，不支持现代框架（React/Vue 等） |
| 数据传输 | XMLHttpRequest | 比 fetch 兼容性更好（Kindle 浏览器引擎老） |
| Kindle 霸屏 | lipc 命令 + killall | 通过系统级命令禁用浏览器 UI、杀掉桌面框架 |

## 四、核心模块设计

### 4.1 正则解析模块（parser.js）

**职责：** 将自然语言文本解析为 `{ date, time, title, note }` 结构。

**解析规则：**

| 类别 | 支持格式 | 示例 |
|------|---------|------|
| 相对日期 | 今天/明天/后天/大后天 | `明天下午3点 开会` |
| 绝对日期 | X月X号 / X月X日 | `10月20号 买菜` |
| 斜杠日期 | MM-DD / MM/DD | `10-20 买菜` / `10/20 买菜` |
| 星期 | 周X / 星期X / 下周X / 下下周X / 这周X | `周五 接孙子` / `下周三 开会` |
| 时间段 | 上午/下午/晚上/中午/早上/清晨 + X点 | `下午3点` → 15:00 |
| 半点 | X点半 | `8点半` → 08:30 |
| 数字时间 | HH:MM | `15:00` |
| 标题 | 去掉日期时间后的剩余文字 | `张医生复查` |
| 备注 | 标题中用空格/逗号分隔的第二部分 | `带环保袋` |

**解析流程：**

```
输入文本
  │
  ├─ 1. 匹配日期（优先级：相对 > 绝对 > 星期 > 默认今天）
  │
  ├─ 2. 匹配时间（优先级：带时段前缀 > 纯数字 HH:MM）
  │
  ├─ 3. 从原文中移除所有已匹配的日期时间片段
  │
  ├─ 4. 剩余文字提取标题（第一段）和备注（第二段）
  │
  └─ 输出 { date, time, title, note, raw }
```

**已验证的解析示例：**

| 输入 | date | time | title | note |
|------|------|------|-------|------|
| 明天下午3点 张医生复查 | 2026-09-16 | 15:00 | 张医生复查 | - |
| 10月20号上午10点 买菜 带环保袋 | 2026-10-20 | 10:00 | 买菜 带环保袋 | - |
| 下周三 下午3点 开会 | 2026-09-16 | 15:00 | 开会 | - |
| 今天晚上8点 吃药 | 2026-09-15 | 20:00 | 吃药 | - |
| 大后天 预约牙医 | 2026-09-18 | - | 预约牙医 | - |
| 周五 下午2点 接孙子 | 2026-09-18 | 14:00 | 接孙子 | - |
| 12月25号 晚上7点 圣诞聚餐 带礼物 | 2026-12-25 | 19:00 | 圣诞聚餐 带礼物 | - |

### 4.2 服务端主服务（server.js）

**职责：** 微信回调接收 + REST API + HTML 页面托管 + SQLite 数据管理。

**模块划分：**

```
server.js
├── 微信回调
│   ├── GET  /wechat          微信服务器签名验证
│   └── POST /wechat          接收用户消息 → 正则解析 → 存库 → 回复确认
│
├── REST API
│   ├── GET    /api/schedules          获取日程列表（支持按日期/天数筛选）
│   ├── POST   /api/schedules          手动添加日程（Web 表单调用）
│   ├── DELETE /api/schedules/:id      删除日程
│   ├── PUT    /api/schedules/:id/done 标记完成
│   └── GET    /api/config             获取配置（轮询间隔等）
│
├── 页面路由
│   ├── GET /dashboard         Kindle 端展示页面
│   └── GET /add               手机端添加页面
│
└── SQLite
    └── schedules 表
        ├── id          主键自增
        ├── date        日期 (YYYY-MM-DD)
        ├── time        时间 (HH:MM)，NULL 表示全天
        ├── title       标题
        ├── note        备注（可空）
        ├── raw         原始消息
        ├── done        完成标记 (0/1)
        └── created_at  创建时间
```

**微信回调处理流程：**

```
微信用户发消息
  ↓
微信服务器 POST /wechat (XML)
  ↓
服务端解析 XML 提取 Content
  ↓
parser.parseMessage(content) 正则解析
  ↓
INSERT INTO schedules
  ↓
构造 XML 回复（含解析结果确认）
  ↓
返回微信服务器
```

### 4.3 Kindle 端展示页面（dashboard.html）

**设计原则：**

- 纯白底纯黑字，e-ink 最大对比度
- Apple 风格：圆角卡片（20px）、药丸标签（980px 圆角）、Helvetica 字体
- 不使用任何外部 CDN 或框架（Kindle 浏览器引擎老）
- 不使用 fetch API（用 XMLHttpRequest 兼容旧引擎）
- 禁用文字选择、右键菜单、拖拽（防误触）

**页面结构：**

```
┌─────────────────────────────────┐
│  [今天]  周一          9月15日   │  ← 顶部日期栏
├─────────────────────────────────┤
│                                 │
│  ┌───────────────────────────┐  │
│  │ 15:00                      │  │
│  │ 张医生复查                  │  │  ← Apple 风格卡片
│  └───────────────────────────┘  │
│                                 │
│  ┌───────────────────────────┐  │
│  │ 20:00                      │  │
│  │ 吃药                        │  │
│  └───────────────────────────┘  │
│                                 │
├─────────────────────────────────┤
│  [←]      第 1 天        [→]   │  ← 底部导航
└─────────────────────────────────┘
```

**交互逻辑：**

| 操作 | 触发 | 效果 |
|------|------|------|
| 左滑 | touchend, deltaX < -80px | currentOffset++, 切到下一天 |
| 右滑 | touchend, deltaX > 80px | currentOffset--, 切到上一天（最小 0） |
| 点击 ← | click 事件 | 同右滑 |
| 点击 → | click 事件 | 同左滑 |
| 定时轮询 | setInterval 30 秒 | XMLHttpRequest 拉取最新数据并渲染 |

**渲染策略：** 每次切换日期或收到新数据时，清空 `#cards` 容器 innerHTML 重新构建。e-ink 全屏刷新一次可接受（不用做局部刷新的复杂处理）。

### 4.4 手机端添加页面（add.html）

**双模式设计：**

| 模式 | 适用场景 | 输入方式 |
|------|---------|---------|
| 自然语言 | 快速输入一句话 | 文本框 + 正则解析，点示例标签快速填充 |
| 表单填写 | 精确填写结构化字段 | 日期选择器 + 时间选择器 + 标题输入框 + 备注输入框 |

**样式特点：**

- Apple 风格表单：圆角分组卡片、980px 圆角药丸切换按钮
- 大字号大按钮，适合老年人操作
- 提交后 Toast 弹窗确认，不跳转页面
- 预置示例标签（点击即填充），降低学习成本

### 4.5 Kindle 霸屏脚本（kiosk.sh）

**三层霸屏策略：**

| 层 | 动作 | 目的 |
|----|------|------|
| 系统层 | `lipc-set-prop` 禁用屏幕休眠 | 保持常亮不锁屏 |
| 进程层 | `killall -9 homeframework` 杀掉桌面框架 | 浏览器成为唯一前台进程，点屏幕任意位置无法回到桌面 |
| 浏览器层 | `lipc-set-prop` 禁用浏览器地址栏和工具栏 | 屏幕上只显示 HTML 页面本身，无任何浏览器 UI |

**守护进程：** kiosk.sh 末尾有一个 `while true` 循环，每 30 秒检测浏览器进程是否存在，崩溃则自动重启。

**恢复方式：** SSH 执行 `sh /mnt/us/recover.sh`，杀掉浏览器和守护进程，重启桌面框架。兜底方案：长按电源键 40 秒强制重启。

### 4.6 恢复脚本（recover.sh）

| 步骤 | 动作 |
|------|------|
| 1 | 杀掉 kiosk.sh 守护进程 |
| 2 | 杀掉浏览器进程 |
| 3 | 恢复休眠功能（lipc） |
| 4 | 重启桌面框架（/etc/init.d/framework start） |

## 五、API 设计

### 5.1 REST API

| 方法 | 路径 | 入参 | 出参 | 说明 |
|------|------|------|------|------|
| GET | `/api/schedules` | 无（默认未来全部） | `[{id,date,time,title,note}]` | 获取今日及之后的日程，按日期和时间排序 |
| GET | `/api/schedules?date=YYYY-MM-DD` | date | 同上 | 获取指定日期日程 |
| GET | `/api/schedules?days=N` | days | 同上 | 获取未来 N 天日程 |
| POST | `/api/schedules` | `{date,time,title,note}` 或 `{raw}` | `{success}` | 添加日程，传 raw 时走正则解析 |
| DELETE | `/api/schedules/:id` | id | `{success}` | 删除日程 |
| PUT | `/api/schedules/:id/done` | id | `{success}` | 标记完成 |
| GET | `/api/config` | 无 | `{pollInterval}` | 获取配置信息 |

### 5.2 微信回调 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/wechat` | 微信服务器签名验证（SHA1） |
| POST | `/wechat` | 接收微信消息 XML → 正则解析 → 存库 → 回复确认 XML |

**微信消息处理流程：**

```
用户发消息 → 微信服务器 POST /wechat (XML)
  → xml2js 解析提取 Content
  → parser.parseMessage() 正则解析
  → INSERT INTO schedules
  → 回复 XML: "已添加日程\n日期: X\n时间: X\n标题: X"
```

## 六、OTA 方案

| 更新对象 | 操作方式 | Kindle 端是否需要操作 | 生效延迟 |
|---------|---------|---------------------|---------|
| 日程内容 | 手机微信发消息或网页表单提交 | 不需要 | 30 秒内（下次轮询） |
| 页面布局 / CSS | 改服务端 `dashboard.html` | 不需要 | 30 秒内（下次轮询） |
| 前端交互逻辑 / JS | 改服务端 `dashboard.html` 的 `<script>` | 不需要 | 30 秒内（下次轮询） |
| 正则解析规则 | 改服务端 `parser.js`，重启服务 | 不需要 | 即时（重启后） |
| 轮询间隔 | 改服务端 `config.js`，重启服务 | 不需要 | 即时（重启后） |
| Kindle kiosk 脚本 | SSH 推送新 `kiosk.sh`，先 recover 再 kiosk | 需要 SSH | 即时 |

**核心设计：** Kindle 端是一个纯展示终端，所有内容和布局变更都在服务端完成，Kindle 端 JS 定时轮询自动获取最新版本，真正实现了「改服务端 → Kindle 自动更新」的 OTA 体验。

## 七、技术约束与已知限制

### 7.1 Kindle 浏览器限制

| 限制 | 影响 | 应对 |
|------|------|------|
| 基于旧版 WebKit | 不支持 ES6+、fetch API、CSS Grid | 用 ES5 语法、XMLHttpRequest、Flexbox |
| JS 性能弱 | 复杂动画会卡顿 | 不做动画，只做 innerHTML 替换 |
| e-ink 刷新慢 | 频繁刷新会闪屏 | 30 秒轮询间隔，切换日期时全屏刷一次 |
| 无现代 CSS 支持 | 不支持 CSS 变量、Grid | 用固定值和 Flexbox |

### 7.2 正则解析限制

| 限制 | 示例 | 应对 |
|------|------|------|
| 不支持复杂语义 | "下个月" / "年底" 无法解析 | 后续可迭代或手动用表单 |
| 备注分隔不够智能 | "买菜 带环保袋" 会被整体当标题 | 需用两个空格或逗号分隔 |
| 日期已过自动转明年 | "1月1号" 当前 9 月 → 明年 1 月 | 有意设计，可调整 |

### 7.3 Kiosk 模式限制

| 限制 | 应对 |
|------|------|
| Kindle 重启后恢复桌面（非持久化） | 理想方案：写入 /etc/init.d/ 启动脚本或 KUAL 配置开机自启 |
| 不同 Kindle 型号浏览器路径不同 | kiosk.sh 中逐个尝试 `/usr/bin/browser`、`/usr/bin/webreader`、`/usr/java/bin/cvm` |
| lipc 命令因型号差异可能失败 | 脚本中每个命令都加了 `2>/dev/null` 容错 |

## 八、项目文件清单

```
kindle-dashboard/
├── server/
│   ├── package.json          依赖声明 (express + sqlite3 + xml2js)
│   ├── config.js             全局配置 (端口/微信Token/轮询间隔)
│   ├── parser.js             正则解析模块 (已验证 10+ 种格式)
│   ├── server.js             主服务 (DB + 微信回调 + REST API + 页面托管)
│   └── views/
│       ├── dashboard.html    Kindle 端展示页面 (Apple 风格卡片 + 触摸交互)
│       └── add.html          手机端添加页面 (自然语言 + 表单双模式)
├── kindle/
│   ├── kiosk.sh              霸屏启动脚本 (杀桌面 + 全屏浏览器 + 守护进程)
│   └── recover.sh            恢复脚本 (杀浏览器 + 重启桌面)
└── README.md                 部署文档
```

## 九、完整使用流程

### 9.1 首次部署

1. 服务端：`cd server && npm install && node server.js`
2. 微信公众号：后台配置回调地址为 `http://<服务器IP>:3000/wechat`
3. Kindle 端：修改 `kiosk.sh` 中 `SERVER_URL`，SSH 传到 Kindle，执行 `sh /mnt/us/kiosk.sh`

### 9.2 日常使用

| 操作 | 谁做 | 怎么做 |
|------|------|--------|
| 新增日程 | 家人（手机） | 微信公众号发消息 `明天下午3点 张医生复查` |
| 新增日程 | 家人（手机） | 浏览器打开 `/add` 页面填写 |
| 查看日程 | Kindle 端 | 自动显示，左右滑动切换日期 |
| 改布局 | 管理者 | 编辑 `dashboard.html` 的 CSS，30 秒后自动生效 |
| 改解析规则 | 管理者 | 编辑 `parser.js`，重启服务 |
| 退出霸屏 | 管理者 | SSH 执行 `sh /mnt/us/recover.sh` |
